{
  "name": "apply-plugin",
  "version": "0.1.0",
  "description": "Chrome extension that records and auto-fills job application forms.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "jsdom": "^24.0.0"
  }
}
